# RoutePilot .env (example)

# Required: Vercel AI Gateway (OpenAI-compatible)
AI_GATEWAY_BASE_URL=
AI_GATEWAY_API_KEY=

# Optional: security and output
# JWT secret used to sign receipt payloads (HMAC-SHA256)
JWT_SECRET=dev-secret
# Also mirror signed receipts to data/receipts/<id>.json
# ROUTEPILOT_MIRROR_JSON=1
# Include input/output snapshots in receipt payloads (enables replay)
# ROUTEPILOT_SNAPSHOT_INPUT=1
# Basic PII redaction in mirrored/snapshot fields
# ROUTEPILOT_REDACT=1
# Comma-separated fields in meta to hard-redact
# ROUTEPILOT_REDACT_FIELDS=secret,token

# Reporting timezone (used by usage/quotas if not set in policy)
# TZ=Asia/Kolkata

# Optional: HTTP fetch tool for demo agents
# HTTP_FETCH_ALLOWLIST=jsonplaceholder.typicode.com
# HTTP_FETCH_URL_TEMPLATE=https://jsonplaceholder.typicode.com/posts/{id}
# HTTP_FETCH_MAX=3

# Demos / toggles
# Simulate primary model stall or 5xx to show fallbacks
# CHAOS_PRIMARY_STALL=1
# CHAOS_HTTP_5XX=1

# Agents/chains helpers
# Probe prompt tokens on non-stream call when headers are absent
# ROUTEPILOT_USAGE_PROBE=1
# Cancel slower branch once one finishes in parallel chains
# ROUTEPILOT_EARLY_STOP=1
# Validate plans/schemas only; no model calls
# ROUTEPILOT_DRY_RUN=1

